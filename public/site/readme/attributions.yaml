# yaml-language-server: $schema=../../../schemas/visual-schema.json

global:
  defaultTheme: light

rows:
  - row:
      maxCols: 1
      theme:
        base: mb-10
        componentHolder:
          decoration: mt-5
      components:
        - core/typography:
            theme:
              contentContainerStyle:
                base: prose prose-amber dark:prose-invert
              containerStyle:
                decoration: container mx-auto
            selector:
              attributes:
                sectionName: attributions

