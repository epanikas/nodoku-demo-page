# yaml-language-server: $schema=../../../../schemas/visual-schema.json

rows:
  - row:
      components:
        - mambaui/hero-one:
            selector:
              attributes:
                id: hero