# yaml-language-server: $schema=../../../../schemas/visual-schema.json

rows:
  - row:
      components:
        - mambaui/hero-left-text:
            selector:
              attributes:
                id: hero