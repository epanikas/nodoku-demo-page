# yaml-language-server: $schema=../../../../schemas/visual-schema.json

rows:
  - row:
      components:
        - mambaui/hero-two:
            selector:
              attributes:
                id: hero